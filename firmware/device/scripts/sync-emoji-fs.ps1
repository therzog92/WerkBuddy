# DEPRECATED — device emojis are baked into firmware (bake_emoji_assets.py).
# LittleFS PNGs are no longer used for WerkPager badges.
Write-Host "Skipped: emojis are compiled into the firmware via scripts/bake_emoji_assets.py"
exit 0
